// import { PayloadAction } from '@reduxjs/toolkit';
// import { put } from 'redux-saga/effects';
// import { setIsConnectedNetwork, setLoading } from '@reducers/app';

// function* setLoadingScreen(action: PayloadAction<boolean>) {
//   yield put(setLoading(action.payload));
// }
//
// function* setNetworkConnectedInternet(action: PayloadAction<boolean>) {
//   yield put(setIsConnectedNetwork(action.payload));
// }
